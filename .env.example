OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
# Repeat questions reuse the last answer (no extra OpenAI calls). Set 0 to disable.
LLM_CACHE=1
# Append one JSON line per request to data/query_traces.jsonl. Set 0 to disable.
OBSERVE=1
